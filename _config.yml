# LocalPibox GitHub Pages site
title: LocalPibox
tagline: Personal AI devstack on Pi.dev
description: A containerized Pi coding agent paired with a local Lemonade/Qwen LLM, MCP servers, and forked extensions.
author: localpibox
email: localpibox@gmail.com

# GitHub Pages honours the theme if it's a supported pages gem
remote_theme: pages-themes/minimal@v0.2.0
plugins:
  - jekyll-remote-theme

# Only build the landing page to keep the site simple
include:
  - README.md

# Optional site niceties
show_downloads: false
